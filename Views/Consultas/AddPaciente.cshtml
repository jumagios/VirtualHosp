@model VirtualHosp.Models.ConsultaRelations

@{
    ViewData["Title"] = "Create";
}

<h1>Agregar paciente</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        @using (Html.BeginForm("AddPacienteSave", "Consultas"))
        {
            @Html.HiddenFor(m => m.ConsultaId)
            <div class="form-group">
            @Html.DropDownList("PacienteId", new SelectList(ViewBag.Pacientes, "Id", "NombreCompleto"), null, new { @class = "form-control" })
            </div>
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-info">Volver</a>
            </div>
        }
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
